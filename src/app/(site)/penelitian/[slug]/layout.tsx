import './detail.css';

/** Shares the detail stylesheet between page and not-found. */
export default function PenelitianDetailLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
